import type { WebPartContext } from "@microsoft/sp-webpart-base";


export interface IReunionesProps {
  // description: string;
  // isDarkTheme: boolean;
  // environmentMessage: string;
  // hasTeamsContext: boolean;
  // userDisplayName: string;
  context: WebPartContext;
  
}
